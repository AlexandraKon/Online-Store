import styled from "styled-components";

export const Nav = () => {

    return (
        <NavContainer>
            <ul>
                <li></li>
            </ul>
        </NavContainer>
    );
}

const NavContainer = styled.nav`
    display: flex;
`;